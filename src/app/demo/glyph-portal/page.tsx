import Demo from "@/components/ui/glyph-portal-demo";

// Standalone preview route for the GlyphPortal component — not linked from
// site nav. Visit directly at /demo/glyph-portal.
export default function GlyphPortalDemoPage() {
  return <Demo />;
}
